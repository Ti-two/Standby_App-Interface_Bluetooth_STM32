<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.StandbyApp" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Couleur de la barre de statut -->
        <item name="android:statusBarColor">#00041A</item>
        <!-- Couleur de la barre de navigation -->
        <item name="android:navigationBarColor">#000000</item>
        <!-- Texte de la barre de statut en clair (pour fond sombre) -->
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>